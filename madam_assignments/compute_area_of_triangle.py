def computeAreaOfTraingle(base, height):
    print('Area:', 0.5 * base * height)

base = float(input('Enter the base for traingle:'))
height = float(input('Enter the height for traingle:'))
computeAreaOfTraingle(base, height)